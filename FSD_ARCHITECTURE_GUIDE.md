# FSD 아키텍처 가이드라인

## 1. 기본 규칙 (Core Principles)

FSD는 **단방향 의존성(Unidirectional Dependency Rule)**을 핵심으로 한다.

의존성 규칙은 다음과 같다:

### 1) 상위 레이어는 하위 레이어를 참조할 수 있다.

```
features → entities
widgets → features
pages → widgets
```

**예시:**
- `features`는 `entities`를 참조 가능
- `widgets`는 `features`를 참조 가능
- `pages`는 `widgets`를 참조 가능

### 2) 동일 계층 간 참조는 허용된다.

```
entities/product <=> entities/user
features/cart <=> features/wishlist
```

**예시:**
- `entities` 간 상호 참조 가능
- `features` 간 상호 참조 가능

### 3) 하위 레이어는 상위 레이어를 참조할 수 없다.

```
entities → features ❌
features → widgets ❌
shared → entities ❌
```

**금지 사항:**
- `entities`는 `features`를 참조할 수 없음
- `features`는 `widgets`를 참조할 수 없음
- `shared`는 `entities`를 참조할 수 없음

이 규칙을 통해 의존성 사이클을 방지하고, 기능이 독립적으로 확장 가능하도록 만든다.

---

## 2. 레이어 정의 (Layers)

각 레이어는 역할이 명확히 구분되어야 하며, 아래 기준을 따른다.

### (2-1) app/

앱 전체에 영향을 주는 글로벌 레벨의 설정과 초기화 코드만 포함한다.

**포함 내용:**
- 글로벌 Router
- Providers 설정 (QueryProvider, ReduxProvider 등)
- 글로벌 스타일/테마 설정
- 앱 초기 부트스트랩 로직

**예시 구조:**
```
app/
  providers/
    query-client-provider.tsx
    router-provider.tsx
  styles/
    global.css
  main.tsx
```

> ⚠️ **주의**: `slice` 계층은 존재하지 않는다.

### (2-2) pages/

각 페이지(Route Entry)를 나타내는 파일만 포함한다.

**특징:**
- 파일 자체에는 로직을 최소화
- 페이지는 `widgets` 또는 `features`를 조합하여 화면을 구성하는 역할

**예시:**
```
pages/
  product/
    index.tsx
  post/
    PostsManagerPage.tsx
```

### (2-3) widgets/

여러 **features 및 entities를 조합**해 중간 단위 UI 구조를 만드는 레이어이다.

**특징:**
- 페이지별 큰 UI 블록
- 여러 도메인의 기능 종합
- "아이폰 위젯"처럼 독립된 하나의 블록 UI

**예시:**
```
widgets/
  post/
    PostTable
    PostAddModal
    PostEditModal
  comment/
    Comments
    CommentItem
  user/
    UserInfoModal
```

> ⚠️ **주의**: `widgets`는 **features + entities + shared(ui)**로 구성된다.

### (2-4) features/

사용자의 **행동(Action)**을 기반으로 한 단일 기능 단위를 정의하는 레이어.

**특징:**
- 비즈니스 엔티티를 조작하거나 사용자 인터랙션에 반응하는 로직 중심
- 사용자 행동에 대한 반응

**예시:**
```
features/
  addToCart/
  toggleWishlist/
  login/
  post/
    usePostFilters
    PostFilters
```

**기능 예시:**
- 찜하기
- 장바구니 추가
- 검색 필터 적용
- 사용자 로그인

### (2-5) entities/

도메인의 비즈니스 모델과 관련된 로직을 포함한다.

**특징:**
- 데이터 모델 정의
- API 요청
- 기본 비즈니스 규칙(validation 등)
- 특정 UI나 사용자 동작을 모르고, 오직 비즈니스 데이터에만 집중

**예시:**
```
entities/
  product/
  user/
  review/
  post/
    api/
    model/
  comment/
    api/
    model/
```

### (2-6) shared/

전역적으로 사용 가능한 "어디에도 속하지 않는" 공통 요소.

**포함 내용:**
- 유틸 함수
- 타입 정의
- 전역 UI 컴포넌트(Button, Modal, Input)
- 전역 스타일, 상수

**예시 구조:**
```
shared/
  ui/
    button.tsx
    dialog.tsx
    input.tsx
  lib/
    formatPrice.ts
    validateEmail.ts
  types/
    common.ts
  config/
    constants.ts
```

> ⚠️ **주의**: 여기에는 도메인 로직이 절대 포함되면 안 된다.

---

## 3. Slice 규칙

슬라이스 폴더명은 **"비즈니스 기준"**으로 정의하며,

- 도메인과 무관한 기능이면 기능 중심으로
- 도메인과 관계 없는 기능이 많을 때에는 역할 중심으로 slice를 생성한다.

**예시:**
```
features/addToCart
entities/product
widgets/productGallery
```

### 🤔 헤더/푸터 같은 글로벌 UI는 어디에?

**선택 기준:**
- 전역적으로 재사용하는 Layout = `shared/ui/layout`
- 페이지 단위 구성에 참여하는 구조 = `widgets/layout`

**권장사항:** 프로젝트 전체에서 통일된 규칙을 선택하여 사용

---

## 4. Segments 규칙 (하위 구조 정리)

각 slice 내부는 아래와 같이 역할 기반으로 구성된다.

### (4-1) ui/

**특징:**
- 순수 UI 컴포넌트
- 비즈니스 로직 포함 금지
- props 기반으로만 작동

**예시:**
```
ui/ProductCard.tsx
ui/CommentItem.tsx
```

### (4-2) api/

**특징:**
- API 통신
- 서버 요청, REST, GraphQL 등
- 엔드포인트 정의

**예시:**
```
api/getProduct.ts
api/updateUser.ts
api/postApi.ts
api/commentApi.ts
```

### (4-3) model/

**특징:**
- 상태 관리(store, signals, atom, reducers)
- 비즈니스 규칙
- 이벤트 핸들러 (openModal, toggleLike 등)

**예시:**
```
model/store.ts
model/events.ts
model/selectors.ts
model/types.ts
```

### (4-4) lib/

**특징:**
- slice 내부에서 사용하는 유틸 함수 모음
- 순수 로직

**예시:**
```
lib/formatPrice.ts
lib/validateEmail.ts
lib/usePostFilters.ts
```

### (4-5) config/

**특징:**
- slice 내 설정
- 상수 값
- 환경 설정

**예시:**
```
config/constants.ts
config/options.ts
```

---

## 5. 의존성 다이어그램

```
┌─────────┐
│  pages  │
└────┬────┘
     │
     ▼
┌─────────┐
│ widgets │
└────┬────┘
     │
     ▼
┌─────────┐
│features │
└────┬────┘
     │
     ▼
┌─────────┐
│entities │
└────┬────┘
     │
     ▼
┌─────────┐
│ shared  │
└─────────┘
```

**의존성 방향:**
- `pages` → `widgets` → `features` → `entities` → `shared`
- 동일 레이어 간 상호 참조 가능
- 역방향 참조 불가

---

## 6. 체크리스트

프로젝트 구조를 검증할 때 다음 사항을 확인하세요:

### ✅ 기본 규칙
- [ ] 상위 레이어가 하위 레이어만 참조하는가?
- [ ] 하위 레이어가 상위 레이어를 참조하지 않는가?
- [ ] 의존성 사이클이 없는가?

### ✅ 레이어 역할
- [ ] `app/`에 글로벌 설정만 있는가?
- [ ] `pages/`에 최소한의 로직만 있는가?
- [ ] `widgets/`가 features와 entities를 조합하는가?
- [ ] `features/`가 사용자 행동을 처리하는가?
- [ ] `entities/`가 비즈니스 모델만 다루는가?
- [ ] `shared/`에 도메인 로직이 없는가?

### ✅ Slice 구조
- [ ] slice 이름이 비즈니스 기준인가?
- [ ] 각 slice 내부에 적절한 segments가 있는가?
- [ ] `ui/`에 비즈니스 로직이 없는가?
- [ ] `api/`에 API 통신만 있는가?
- [ ] `model/`에 상태 관리와 비즈니스 규칙이 있는가?

---

## 7. 참고 자료

- [Feature-Sliced Design 공식 문서](https://feature-sliced.design/)
- [FSD Best Practices](https://feature-sliced.design/docs/get-started/overview)

---

**마지막 업데이트:** 2024년

